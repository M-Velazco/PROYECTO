name: flutter_application_1
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  mysql1: ^0.20.0

  # Añadir dependencias de Firebase
  firebase_core: ^2.10.0
  firebase_auth: ^4.8.2

  cupertino_icons: ^1.0.6
  cloud_firestore: ^4.17.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Aquí puedes agregar activos si los necesitas, por ejemplo:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg